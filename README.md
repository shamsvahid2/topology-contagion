## Pipline:

To get results you need the to do the following steps:
1. firstly run the simulation notebooks (simulations_unweighted, simulations_weighted) 
2. then run the `EPH` notebook to compute extended persistent homology and corr (PRL method).
3. Finally for visualizations, you need to run results notebooks.